<!DOCTYPE html>
<html lang="ja">
<div>
  <%= flash[:notice] %>
</div>
<div class="body2">
<h1 class="book">Books</h1>

<table>
    <tr class="table">
      <th class="mar">Title</th>
      <th class="mar">Body</th>
      <th colspan='3'></th>
    </tr>
     <% @books.each do |book| %>
    <tr>
      <td class="tabler"><%= book.title %></td>
      <td class="tabler"><%= book.body %></td>
      <td class="tabler"><%= link_to "Show", book_path(book.id) %></td>
      <td class="tabler"><%= link_to "Edit", edit_book_path(book.id) %></td>
      <td class="tabler"><%= link_to "Destroy", book_path(book.id), method: :delete, "data-confirm" => "Are you sure？" %></td>

    </tr>
     <% end %>
</table>
<h1 class="newbook">New book</h1>
 <% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
<%= form_with model: @book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>
  <h5>
  <%= f.submit 'Create Book' %>
  </h5>
<% end %>
</div>
</html>